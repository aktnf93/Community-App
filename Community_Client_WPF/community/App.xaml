<Application x:Class="community.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:community"
             xmlns:v="clr-namespace:community.Views"
             xmlns:vm="clr-namespace:community.ViewModels"
             xmlns:c="clr-namespace:community.Common"
             xmlns:cnv="clr-namespace:community.Converters"
             StartupUri="Views/V_MainWindow.xaml"
             ShutdownMode="OnLastWindowClose"
             >
    <Application.Resources>

        <DataTemplate DataType="{x:Type vm:VM_Board}">
            <v:V_Board/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:VM_Chat}">
            <v:V_Chat/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:VM_Employee}">
            <v:V_Employee/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:VM_Inventory}">
            <v:V_Inventory/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:VM_Messages}">
            <v:V_Mseeage/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:VM_Project}">
            <v:V_Project/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:VM_Settings}">
            <v:V_Settings/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:VM_TEST}">
            <v:V_TEST/>
        </DataTemplate>





        <!-- 남색 테마 색상 -->
        <SolidColorBrush x:Key="NavyBrush" Color="#FF1A237E"/>
        <SolidColorBrush x:Key="WhiteBrush" Color="White"/>
        <Style x:Key="CommentItemStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFF5F7FF"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Margin" Value="0,2,0,2"/>
            <Setter Property="Padding" Value="8"/>
        </Style>

        <!-- ComboBox 스타일 -->
        <Style TargetType="ComboBox" x:Key="ComboStyle1">
            <Setter Property="Foreground" Value="#FF333333"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFF5A623"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Height="24">
                            <Grid>
                                <ToggleButton Name="ToggleButton" Grid.Column="2" Focusable="false"
                                              IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              ClickMode="Press" Background="Transparent" BorderThickness="0"
                                              HorizontalContentAlignment="Right"
                                              VerticalContentAlignment="Center">
                                    
                                    <Path x:Name="Arrow" Data="M 0 0 L 4 4 L 8 0 Z" Fill="#FFB22222" Margin="0,0,6,0"/>
                                </ToggleButton>
                                
                                <ContentPresenter Margin="6,0,25,0" VerticalAlignment="Center" HorizontalAlignment="Left" Content="{TemplateBinding SelectionBoxItem}"
                                                  IsHitTestVisible="False"/>
                                
                                <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                    <Border Background="White" BorderBrush="#FFF5A623" BorderThickness="1" CornerRadius="4">
                                        <ScrollViewer>
                                            <StackPanel IsItemsHost="True" />
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ToggleButton 스타일 -->
        <Style TargetType="ToggleButton" x:Key="ToggleStyle1">
            <Setter Property="Foreground" Value="#FF333333"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFF5A623"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#FFB22222"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#FFB22222"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button 스타일 -->
        <Style TargetType="Button" x:Key="ButtonStyle1">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#FFB22222"/>
            <Setter Property="BorderBrush" Value="#FFF5A623"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFF5A623"/>
                                <Setter Property="Foreground" Value="#FFB22222"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF8B1A1A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FFE0E0E0"/>
                                <Setter Property="Foreground" Value="#FFAAAAAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate TargetType="{x:Type RadioButton}" x:Key="RadioTemplate">
            <Border x:Name="bor" Width="30" Height="24"
                    Background="#FF1A237E"
                    BorderBrush="Gray" BorderThickness="0.5" CornerRadius="3">
                <Grid>
                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                      TextBlock.FontWeight="Bold"
                                      TextBlock.Foreground="White"/>
                </Grid>
            </Border>
            
            <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="bor" Property="Background" Value="#FFB22222"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="bor" Property="BorderBrush" Value="#FFB22222"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <ControlTemplate TargetType="{x:Type RadioButton}" x:Key="MenuButton">
            <Border x:Name="bor" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}"
                    Background="{TemplateBinding Background}"
                    BorderBrush="Transparent" BorderThickness="0" CornerRadius="0">
                <Grid>
                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                      TextBlock.FontWeight="Bold"
                                      TextBlock.Foreground="White"/>
                </Grid>
            </Border>

            <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="bor" Property="Background" Value="#FFB22222"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="bor" Property="BorderBrush" Value="#FFB22222"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <ControlTemplate TargetType="{x:Type Button}" x:Key="ButtonTemplate">
            <Border x:Name="bor" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}"
                    Background="#FF1A237E"
                    BorderBrush="Gray" BorderThickness="0.5" CornerRadius="3">
                <Grid>
                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                      TextBlock.FontWeight="Bold"
                                      TextBlock.Foreground="White"/>
                </Grid>
            </Border>

            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="bor" Property="Background" Value="#FFB22222"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="bor" Property="Background" Value="#FFB22222"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="{x:Type ProgressBar}" x:Key="LinePoint_ProgressBar">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="Root" Background="Transparent">
                            <!-- 회색 배경 -->
                            <Border Background="#DDD" CornerRadius="2" Height="6" VerticalAlignment="Center"/>

                            <!-- 진행된 영역 -->
                            <Rectangle x:Name="TrackFill" Fill="#4CAF50" Width="0" Height="6" HorizontalAlignment="Left" VerticalAlignment="Center"/>

                            <!-- 동그라미 Thumb -->
                            <Ellipse x:Name="Thumb" Width="18" Height="18" Fill="White" Stroke="#4CAF50" StrokeThickness="1"
                                     HorizontalAlignment="Left" VerticalAlignment="Center" Margin="-9,0,0,0"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Employee_Status" TargetType="ContentControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentControl">
                        <Border x:Name="Outer" CornerRadius="8" Background="#EEE">
                            <TextBlock x:Name="StatusText" Text="알 수 없음" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <DataTrigger Binding="{Binding Status}" Value="1">
                                <Setter TargetName="Outer" Property="Background" Value="#DFFFD6"/>
                                <Setter TargetName="StatusText" Property="Text" Value="활동 중"/>
                                <Setter TargetName="StatusText" Property="Foreground" Value="Green"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Status}" Value="2">
                                <Setter TargetName="Outer" Property="Background" Value="#FFF3CD"/>
                                <Setter TargetName="StatusText" Property="Text" Value="휴직 중"/>
                                <Setter TargetName="StatusText" Property="Foreground" Value="DarkOrange"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Status}" Value="3">
                                <Setter TargetName="Outer" Property="Background" Value="#F8D7DA"/>
                                <Setter TargetName="StatusText" Property="Text" Value="퇴사"/>
                                <Setter TargetName="StatusText" Property="Foreground" Value="Red"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>




        <!-- 컨버터 -->
        <cnv:PageNumberToIsCheckedConverter x:Key="PageNumberToIsCheckedConverter"/>
        <cnv:PageNumberToIsCheckedMultiConverter x:Key="PageNumberToIsCheckedMultiConverter"/>
        <cnv:EnumToBoolConverter x:Key="EnumToBoolConverter"/>

    </Application.Resources>
</Application>
