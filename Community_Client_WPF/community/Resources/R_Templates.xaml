<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:v="clr-namespace:community.Views"
                    xmlns:u="clr-namespace:community.View_Controls">

    <!--#region 메뉴 화면 별 배너 -->
    <ControlTemplate TargetType="{x:Type Control}" x:Key="BannerCard">
        <Border Background="{StaticResource Color_ThinGray}" 
                BorderBrush="Gray" BorderThickness="1" CornerRadius="8">

            <TextBlock Text="{Binding Title}" 
                       FontSize="32" FontWeight="Bold" 
                       Foreground="{StaticResource Color_WhiteGray}" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </ControlTemplate>
    <!--#endregion-->

    <!--#region 메뉴 화별 별 대시보드 카드 -->
    <ControlTemplate TargetType="{x:Type Control}" x:Key="DashboardCard">
        <Border x:Name="Bor" CornerRadius="8" Width="218" BorderBrush="{StaticResource Color_LightGray}" BorderThickness="0.5">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,2" EndPoint="1,0">
                    <GradientStop Color="#808080" Offset="0.35"/>
                    <GradientStop Color="#4A4A4A" Offset="0.35"/>
                    <GradientStop Color="#4A4A4A" Offset="0.93"/>
                    <GradientStop Color="#808080" Offset="0.93"/>
                </LinearGradientBrush>
            </Border.Background>

            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Image     Grid.Column="0" Grid.Row="0" Source="{TemplateBinding Tag}" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant"/>
                <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Title}"       Foreground="{StaticResource Color_White}"/>
                <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Content}"     Foreground="{StaticResource Color_White}" FontSize="16" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Grid.Row="2" Text="{Binding Description}" Foreground="{StaticResource Color_White}"/>
            </Grid>
        </Border>
    </ControlTemplate>
    <!--#endregion-->

    <!--#region  기본 버튼 템플릿 -->
    <ControlTemplate TargetType="{x:Type Button}" x:Key="BaseButton">
        <Grid Cursor="Hand">
            <Border x:Name="Border" CornerRadius="4" Background="{StaticResource Color_ThinGray}"/>
            <Image x:Name="Img" Source="{TemplateBinding Tag}" HorizontalAlignment="Left" VerticalAlignment="Center"
                       Width="20" Height="20" Margin="8,0,0,0" Visibility="Visible"/>
            <ContentPresenter x:Name="Button_Text" Content="{TemplateBinding Content}" 
                                  HorizontalAlignment="Center" VerticalAlignment="Center" 
                                  TextBlock.Foreground="{StaticResource Color_White}"/>
        </Grid>

        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Border" Property="Background" Value="Gray"/>
            </Trigger>
            <Trigger Property="Tag" Value="{x:Null}">
                <Setter TargetName="Img" Property="Visibility" Value="Collapsed"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type Button}">
        <Setter Property="Template" Value="{StaticResource BaseButton}"/>
    </Style>
    <!--#endregion-->

    <!--#region 아이콘 버튼 템플릿 -->
    <ControlTemplate TargetType="{x:Type Button}" x:Key="IconButton">
        <Border x:Name="Border" Background="{StaticResource Color_Transparent}" Cursor="Hand">
            <Image Source="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Border" Property="Background" Value="{StaticResource Color_ThinGray}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <!--#endregion-->

    <!--#region 메인 메뉴 라디오 버튼 템플릿-->
    <ControlTemplate TargetType="{x:Type RadioButton}" x:Key="MenuButton">
        <Border x:Name="Border" Height="90" Cursor="Hand" Background="{StaticResource Color_Transparent}"
                    CornerRadius="6" Margin="2,0,2,0">
            <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="8">
                <Image Source="{TemplateBinding Tag}" Width="34" Height="34" Margin="0,0,0,8"/>
                <ContentPresenter x:Name="ButtonText" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                      TextBlock.FontSize="14" TextBlock.FontFamily="고도 M" TextBlock.Foreground="{StaticResource Color_White}"/>
            </StackPanel>
        </Border>

        <ControlTemplate.Triggers>
            <!-- 선택함 -->
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Border" Property="Background" Value="{StaticResource Color_ThinGray}"/>
                <Setter TargetName="ButtonText" Property="TextBlock.Foreground" Value="{StaticResource Color_White}"/>
            </Trigger>

            <!-- 마우스 -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Border" Property="Background" Value="{StaticResource Color_LightGray}"/>
                <Setter TargetName="ButtonText" Property="TextBlock.Foreground" Value="{StaticResource Color_Black}"/>
            </Trigger>

            <!-- 선택 + 마우스 -->
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsChecked" Value="True"/>
                    <Condition Property="IsMouseOver" Value="True"/>
                </MultiTrigger.Conditions>
                <MultiTrigger.Setters>
                    <Setter TargetName="Border" Property="Background" Value="{StaticResource Color_ThinGray}"/>
                    <Setter TargetName="ButtonText" Property="TextBlock.Foreground" Value="{StaticResource Color_White}"/>
                </MultiTrigger.Setters>
            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <!--#endregion-->

    <!--#region 오디오 플레이어 버튼 템플릿 -->
    <ControlTemplate TargetType="{x:Type Button}" x:Key="ButtonTemplate">
        <Border x:Name="bor" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}"
                    Background="#FF1A237E"
                    BorderBrush="Gray" BorderThickness="0.5" CornerRadius="3">
            <Grid>
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                      TextBlock.FontWeight="Bold"
                                      TextBlock.Foreground="White"/>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bor" Property="Background" Value="#FFB22222"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="bor" Property="Background" Value="#FFB22222"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <!--#endregion-->

    <!--#region 게시글 페이지 버튼 템플릿 -->
    <ControlTemplate TargetType="{x:Type RadioButton}" x:Key="RadioTemplate">
        <Border x:Name="bor" Width="30" Height="24"
                    Background="#FF1A237E"
                    BorderBrush="Gray" BorderThickness="0.5" CornerRadius="3">
            <Grid>
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                      TextBlock.FontWeight="Bold"
                                      TextBlock.Foreground="White"/>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="bor" Property="Background" Value="#FFB22222"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bor" Property="BorderBrush" Value="#FFB22222"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <!--#endregion-->


    <ControlTemplate TargetType="{x:Type TextBox}" x:Key="BaseTextBox">
        <Border x:Name="Border" BorderBrush="Gray" BorderThickness="1" CornerRadius="2" Background="White">
            <!-- 입력 영역 -->
            <Grid>
                <ScrollViewer x:Name="PART_ContentHost" Margin="4,0,0,0" VerticalAlignment="Center"/>

                <!-- Placeholder 텍스트 -->
                <TextBlock x:Name="PlaceholderText" Text="{TemplateBinding ToolTip}" 
                                Foreground="Gray" Margin="4,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="Collapsed"/>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="Text" Value="">
                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="DodgerBlue"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="DarkGray"/>
                <Setter TargetName="Border" Property="BorderBrush" Value="Gray"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Template" Value="{StaticResource BaseTextBox}"/>
    </Style>

    <!--#region 아이콘 텍스트박스 -->
    <ControlTemplate TargetType="{x:Type TextBox}" x:Key="Icon_TextBox">
        <Border x:Name="Border" BorderBrush="Gray" BorderThickness="1" CornerRadius="2" Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 아이콘 -->
                <Image Source="{TemplateBinding Tag}" Width="20" Height="20" Margin="8,0,4,0" VerticalAlignment="Center"
                           RenderOptions.BitmapScalingMode="Fant"/>

                <!-- 입력 영역 -->
                <Grid Grid.Column="1">
                    <ScrollViewer x:Name="PART_ContentHost" Margin="0" VerticalAlignment="Center"/>

                    <!-- Placeholder 텍스트 -->
                    <TextBlock x:Name="PlaceholderText" Text="{TemplateBinding ToolTip}" 
                                   Foreground="Gray" Margin="4,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="Collapsed"/>
                </Grid>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="Text" Value="">
                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="DodgerBlue"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="DarkGray"/>
                <Setter TargetName="Border" Property="BorderBrush" Value="Gray"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <!--#endregion-->

    <!--#region 아이콘 패스워드박스-->
    <ControlTemplate TargetType="{x:Type PasswordBox}" x:Key="Icon_PasswordBox">
        <Border x:Name="Border" BorderBrush="Gray" BorderThickness="1" CornerRadius="2" Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 아이콘 -->
                <Image Source="{TemplateBinding Tag}" Width="20" Height="20" Margin="8,0,4,0" VerticalAlignment="Center"/>

                <!-- 입력 영역 + Placeholder -->
                <Grid Grid.Column="1">
                    <ScrollViewer x:Name="PART_ContentHost" Margin="0" VerticalAlignment="Center"/>

                    <!-- Placeholder 텍스트 -->
                    <TextBlock x:Name="PlaceholderText" Text="{TemplateBinding ToolTip}" 
                                   Foreground="Gray" Margin="4,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="Collapsed"/>
                </Grid>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="u:PasswordBoxHelper.Password" Value="">
                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="DodgerBlue"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Border" Property="BorderBrush" Value="Gray"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <!--#endregion-->

    <!--#region 라인포인트 프로그래스바 스타일-->
    <ControlTemplate TargetType="{x:Type ProgressBar}" x:Key="LinePoint_ProgressBar">
        <Grid x:Name="Root">
            <!-- 회색 배경 -->
            <Border Background="#DDD" CornerRadius="2" Height="6" VerticalAlignment="Center"/>

            <!-- 진행된 영역 -->
            <Rectangle x:Name="TrackFill" Fill="#4CAF50" Width="0" Height="6" HorizontalAlignment="Left" VerticalAlignment="Center"/>

            <!-- 동그라미 Thumb -->
            <Ellipse x:Name="Thumb" Width="18" Height="18" Fill="White" Stroke="#4CAF50" StrokeThickness="1"
                            HorizontalAlignment="Left" VerticalAlignment="Center" Margin="-9,0,0,0"/>
        </Grid>
    </ControlTemplate>
    <!--#endregion-->

    <ControlTemplate TargetType="{x:Type Control}" x:Key="CategorySearch">
        <StackPanel  Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top">
            <TextBox Width="156" Height="30"
                         Template="{StaticResource Icon_TextBox}" ToolTip="{TemplateBinding ToolTip}" Margin="0,0,4,0"
                         Tag="{StaticResource Img_Edit}" Text="{Binding TxtCategorySearch}"/>
            <Button Content="검색" Width="80" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top"
                        Tag="{StaticResource Img_Search_w}" Click="{v:MethodBinding BtnCategorySearch}"/>
        </StackPanel>
    </ControlTemplate>

    <ControlTemplate TargetType="{x:Type Control}" x:Key="ListSearch">
        <StackPanel  Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top">
            <ComboBox Width="120" Height="30" Margin="0,0,4,0"
                          ItemsSource="{Binding CboListSearch}" SelectedValue="{Binding CboListSearchSelected}"/>
            <TextBox Width="300" Height="30" 
                         Template="{StaticResource Icon_TextBox}" ToolTip="{TemplateBinding ToolTip}" Margin="0,0,4,0"
                         Tag="{StaticResource Img_Edit}" Text="{Binding TxtListSearch}"/>
            <Button Content="검색" Width="80" Height="30"
                        Tag="{StaticResource Img_Search_w}" Click="{v:MethodBinding BtnListSearch}"/>
        </StackPanel>
    </ControlTemplate>



    <ControlTemplate TargetType="{x:Type Control}" x:Key="StatusBox_Project">
        <Border CornerRadius="10">
            <Border.Style>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="Background" Value="Gray"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Status}" Value="대기">
                            <Setter Property="Background" Value="Gray"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Status}" Value="진행">
                            <Setter Property="Background" Value="Green"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Status}" Value="완료">
                            <Setter Property="Background" Value="Blue"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Status}" Value="보류">
                            <Setter Property="Background" Value="Orange"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Status}" Value="취소">
                            <Setter Property="Background" Value="Red"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <TextBlock Text="{Binding Status}" 
                       Foreground="{StaticResource Color_White}" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
        <ControlTemplate.Triggers>

        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--#region Status : Employee-->
    <ControlTemplate TargetType="{x:Type ContentControl}" x:Key="StatusBox_Employee">
        <Border x:Name="Outer" CornerRadius="8" Background="#EEE">
            <TextBlock x:Name="StatusText" Text="알 수 없음" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding Status}" Value="1">
                <Setter TargetName="Outer" Property="Background" Value="#DFFFD6"/>
                <Setter TargetName="StatusText" Property="Text" Value="활동 중"/>
                <Setter TargetName="StatusText" Property="Foreground" Value="Green"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Status}" Value="2">
                <Setter TargetName="Outer" Property="Background" Value="#FFF3CD"/>
                <Setter TargetName="StatusText" Property="Text" Value="휴직 중"/>
                <Setter TargetName="StatusText" Property="Foreground" Value="DarkOrange"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Status}" Value="3">
                <Setter TargetName="Outer" Property="Background" Value="#F8D7DA"/>
                <Setter TargetName="StatusText" Property="Text" Value="퇴사"/>
                <Setter TargetName="StatusText" Property="Foreground" Value="Red"/>
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <!--#endregion-->

    <ControlTemplate TargetType="{x:Type ComboBox}" x:Key="BaseComboBox">
        <Grid>
            <!-- 메인 영역 -->
            <Border CornerRadius="2" 
                    BorderBrush="{StaticResource Color_Gray}" BorderThickness="1" 
                    Background="{StaticResource Color_White}">
                <Grid>
                    <!-- 드롭다운 버튼 -->
                    <ToggleButton x:Name="PART_ToggleButton" Focusable="False"
                                  Cursor="Hand" Margin="1.8"
                                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                  Background="Transparent" BorderThickness="0"
                                  HorizontalContentAlignment="Right">
                        <Path Margin="0,0,4,0" Style="{StaticResource PathDropDown}"/>
                    </ToggleButton>

                    <!-- 선택 항목 표시 (편집 불가 모드) -->
                    <ContentPresenter x:Name="ContentSite" Focusable="False" IsHitTestVisible="False"
                             Content="{TemplateBinding SelectionBoxItem}"
                             ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                             ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                             HorizontalAlignment="Left" VerticalAlignment="Center" Margin="6,0,30,0"
                             Visibility="Visible"/>

                    <!-- 편집 가능 모드 -->
                    <TextBox x:Name="PART_EditableTextBox"
                             Style="{x:Null}" Background="Transparent" 
                             BorderBrush="Gray" BorderThickness="0.5"
                             HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="6,0,26,0" 
                             Visibility="Collapsed"/>

                    <TextBlock x:Name="PlaceholderText" Text="{TemplateBinding ToolTip}" 
                               HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="6,0,26,0" 
                               Foreground="Gray" IsHitTestVisible="False" Visibility="Collapsed"/>
                </Grid>
            </Border>

            <!-- 드롭다운 목록 -->
            <Popup x:Name="PART_Popup"
                   IsOpen="{TemplateBinding IsDropDownOpen}"
                   Placement="Bottom" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide"
                   MinWidth="{TemplateBinding ActualWidth}">
                <Border CornerRadius="6" Background="White" BorderBrush="{StaticResource Color_Gray}" BorderThickness="1" Padding="4">
                    <ScrollViewer>
                        <ItemsPresenter x:Name="ItemsPresenter"/>
                    </ScrollViewer>
                </Border>
            </Popup>
        </Grid>

        <ControlTemplate.Triggers>
            <Trigger Property="IsEditable" Value="True">
                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                <Setter TargetName="ContentSite" Property="Visibility" Value="Collapsed"/>
            </Trigger>
            <Trigger Property="SelectedItem" Value="{x:Null}">
                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type ComboBox}">
        <Setter Property="Template" Value="{StaticResource BaseComboBox}"/>
    </Style>




    <ControlTemplate TargetType="{x:Type Control}" x:Key="ProjectBlock">
        <Grid x:Name="TaskGrid" Height="46">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60" Tag="ID"/>
                <ColumnDefinition Width="*"  Tag="Name"/>
                <ColumnDefinition Width="190" Tag="Status"/>
                <ColumnDefinition Width="190" Tag="Date"/>

                <ColumnDefinition Width="50" Tag="1"/>
                <ColumnDefinition Width="50" Tag="2"/>
                <ColumnDefinition Width="50" Tag="3"/>
                <ColumnDefinition Width="50" Tag="4"/>
                <ColumnDefinition Width="50" Tag="5"/>
                <ColumnDefinition Width="50" Tag="6"/>
                <ColumnDefinition Width="50" Tag="7"/>
                <ColumnDefinition Width="50" Tag="8"/>
                <ColumnDefinition Width="50" Tag="9"/>
                <ColumnDefinition Width="50" Tag="10"/>
                <ColumnDefinition Width="50" Tag="11"/>
                <ColumnDefinition Width="50" Tag="12"/>

                <ColumnDefinition Width="120" Tag="Member"/>
                <ColumnDefinition Width="90" Tag="Update"/>
                <ColumnDefinition Width="200" Tag="Action"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" x:Name="TxtId" Text="{Binding Id}" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <Grid Grid.Column="1">
                <TextBlock TextTrimming="CharacterEllipsis" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0">
                        <Run Text="{Binding Name}" FontSize="14" FontWeight="Bold"/>
                        <LineBreak/>
                        <Run Text="{Binding Description, StringFormat={}desc: {0}}" FontSize="12"/>
                </TextBlock>

                <ContentControl Template="{StaticResource StatusBox_Project}" 
                                    Width="50" Height="20" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
            </Grid>

            <Grid Grid.Column="2">
                <u:U_ProgressBar Value="{Binding Progress}" Minimum="0" Maximum="100" Width="130" Height="20"
                                     HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0"/>
                <TextBlock Text="{Binding Progress, StringFormat={}{0} %}" 
                               HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
            </Grid>

            <TextBlock Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Run Text="{Binding Start_Date, StringFormat={}{0:yyyy-MM-dd}}"/>
                    <Run Text="~"/>
                    <Run Text="{Binding End_Date, StringFormat={}{0:yyyy-MM-dd}}"/>
            </TextBlock>

            <Border Grid.Column="4"  Background="{Binding Month[0].Color}"  Margin="0"/>
            <Border Grid.Column="5"  Background="{Binding Month[1].Color}"  Margin="0"/>
            <Border Grid.Column="6"  Background="{Binding Month[2].Color}"  Margin="0"/>
            <Border Grid.Column="7"  Background="{Binding Month[3].Color}"  Margin="0"/>
            <Border Grid.Column="8"  Background="{Binding Month[4].Color}"  Margin="0"/>
            <Border Grid.Column="9"  Background="{Binding Month[5].Color}"  Margin="0"/>
            <Border Grid.Column="10" Background="{Binding Month[6].Color}"  Margin="0"/>
            <Border Grid.Column="11" Background="{Binding Month[7].Color}"  Margin="0"/>
            <Border Grid.Column="12" Background="{Binding Month[8].Color}"  Margin="0"/>
            <Border Grid.Column="13" Background="{Binding Month[9].Color}"  Margin="0"/>
            <Border Grid.Column="14" Background="{Binding Month[10].Color}" Margin="0"/>
            <Border Grid.Column="15" Background="{Binding Month[11].Color}" Margin="0"/>


            <!--<TextBlock Grid.Column="16" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Run Text="제임슨"/>
                                <Run Text="차장"/>
                                <LineBreak/>
                                <Run Text="개발부"/>
                                <Run Text="팀장"/>
                </TextBlock>-->

            <TextBlock Grid.Column="17" Text="{Binding Updated_At, StringFormat={}{0:yyyy-MM-dd}}" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <StackPanel Grid.Column="18" x:Name="BtnPanel" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="▼" Width="30" Height="32" Click="{v:MethodBinding BtnOnProjectTaskShow}"/>
                <Button Content="✏️ 편집" Width="70" Height="32" Margin="8,0,4,0" Click="{v:MethodBinding BtnOnProjectEdit}"/>
                <Button Content="➖️ 삭제" Width="70" Height="32" Click="{v:MethodBinding BtnOnProjectDelete}"/>
            </StackPanel>

            <Line x:Name="TopLine" Grid.Column="0" Grid.ColumnSpan="20" Grid.Row="0" 
                  VerticalAlignment="Top" X1="0" X2="100" Stretch="Fill" StrokeThickness="0.5" Stroke="Gray"
                  Visibility="Collapsed"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="Tag" Value="Task">
                <Setter TargetName="TxtId" Property="Visibility" Value="Collapsed"/>
                <Setter TargetName="BtnPanel" Property="Visibility" Value="Collapsed"/>
                <Setter TargetName="TaskGrid" Property="Height" Value="30"/>
                <Setter TargetName="TopLine" Property="Visibility" Value="Visible"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

</ResourceDictionary>