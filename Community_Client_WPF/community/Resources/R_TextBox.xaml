<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:u="clr-namespace:community.View_Controls">

    <!-- 기본 텍스트 박스 -->
    <ControlTemplate TargetType="{x:Type TextBox}" x:Key="BaseTextBox">
        <Border x:Name="Border" BorderBrush="Gray" BorderThickness="1" CornerRadius="2" Background="White">
            <!-- 입력 영역 -->
            <Grid>
                <ScrollViewer x:Name="PART_ContentHost" Margin="4,0,0,0" VerticalAlignment="Center"/>

                <!-- Placeholder 텍스트 -->
                <TextBlock x:Name="PlaceholderText" Text="{TemplateBinding ToolTip}" Foreground="{StaticResource Color_Gray}" Margin="4,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="Collapsed"/>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="Text" Value="">
                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="DodgerBlue"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="DarkGray"/>
                <Setter TargetName="Border" Property="BorderBrush" Value="Gray"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- 아이콘 텍스트 박스 -->
    <ControlTemplate TargetType="{x:Type TextBox}" x:Key="Icon_TextBox">
        <Border x:Name="Border" BorderBrush="{StaticResource Color_Gray}" BorderThickness="1" CornerRadius="2" Background="{StaticResource Color_White}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 아이콘 -->
                <Image Source="{TemplateBinding Tag}" Width="20" Height="20" Margin="8,0,4,0" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="Fant"/>

                <!-- 입력 영역 -->
                <Grid Grid.Column="1">
                    <ScrollViewer x:Name="PART_ContentHost" Margin="0" VerticalAlignment="Center"/>

                    <!-- Placeholder 텍스트 -->
                    <TextBlock x:Name="PlaceholderText" Text="{TemplateBinding ToolTip}"  Foreground="{StaticResource Color_Gray}" Margin="4,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="Collapsed"/>
                </Grid>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="Text" Value="">
                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="DodgerBlue"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="DarkGray"/>
                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource Color_Gray}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- 아이콘 패스워드 박스 -->
    <ControlTemplate TargetType="{x:Type PasswordBox}" x:Key="Icon_PasswordBox">
        <Border x:Name="Border" BorderBrush="{StaticResource Color_Gray}" BorderThickness="1" CornerRadius="2" Background="{StaticResource Color_White}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 아이콘 -->
                <Image Source="{TemplateBinding Tag}" Width="20" Height="20" Margin="8,0,4,0" VerticalAlignment="Center"/>

                <!-- 입력 영역 + Placeholder -->
                <Grid Grid.Column="1">
                    <ScrollViewer x:Name="PART_ContentHost" Margin="0" VerticalAlignment="Center"/>

                    <!-- Placeholder 텍스트 -->
                    <TextBlock x:Name="PlaceholderText" Text="{TemplateBinding ToolTip}" 
                               Foreground="{StaticResource Color_Gray}" Margin="4,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="Collapsed"/>
                </Grid>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="u:PasswordBoxHelper.Password" Value="">
                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="DodgerBlue"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource Color_Gray}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Template" Value="{StaticResource BaseTextBox}"/>
    </Style>

</ResourceDictionary>