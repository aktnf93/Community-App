<UserControl x:Class="community.Views.V_ApprovalList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:community.Views"
             mc:Ignorable="d" 
             Width="1810" Height="950"
             Background="{StaticResource Color_WhiteGray}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30"  Tag="여백"/>
            <ColumnDefinition Width="280" Tag="그룹"/>
            <ColumnDefinition Width="30"  Tag="여백"/>
            <ColumnDefinition Width="*"   Tag="리스트"/>
            <ColumnDefinition Width="30"  Tag="여백"/>
            <ColumnDefinition Width="440" Tag="디테일"/>
            <ColumnDefinition Width="30"  Tag="여백"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"  Tag="타이틀"/>
            <RowDefinition Height="10"  Tag="여백"/>
            <RowDefinition Height="100" Tag="컨트롤"/>
            <RowDefinition Height="50"  Tag="여백"/>
            <RowDefinition Height="*"   Tag="리스트"/>
            <RowDefinition Height="30"  Tag="여백"/>
        </Grid.RowDefinitions>

        <!-- 타이틀 -->
        <Grid Grid.Column="1" Grid.ColumnSpan="5" Grid.Row="0" TextBlock.FontFamily="고도 M">
            <TextBlock Text="결재문서" FontSize="24" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
        </Grid>

        <!-- 중간 컨트롤 -->
        <Border Grid.Column="3" Grid.ColumnSpan="3" Grid.Row="2" Style="{StaticResource BaseCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 필터 체크 박스 -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="검색옵션" FontSize="13" FontWeight="Bold" Width="50" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    <CheckBox Content="전체" IsChecked="True" Margin="20,0,0,0"/>
                    <CheckBox Content="휴가 신청서" Margin="20,0,0,0"/>
                    <CheckBox Content="지출 결의서" Margin="20,0,0,0"/>
                </StackPanel>

                <Line Grid.Row="0" Stroke="{StaticResource Color_Gray}" StrokeThickness="0.5" X1="0" X2="2000" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"/>

                <!-- 이름 검색 -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="검색" FontSize="13" FontWeight="Bold" Width="50" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,0,20,0"/>

                    <StackPanel Orientation="Horizontal" Width="300" VerticalAlignment="Center">
                        <TextBox Width="200" Height="30" Template="{StaticResource Icon_TextBox}" Tag="{StaticResource Img_Edit}" ToolTip="기안자"/>
                        <Button Width="30" Height="30" Content="🔍︎" Margin="6,0,0,0" ToolTip="검색" Click="{local:MethodBinding BtnApprovalSearch}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 결재 문서 타입 별 진행 -->
        <Border Grid.Column="1" Grid.Row="2" Grid.RowSpan="3" Style="{StaticResource BaseCard}">
            <Border.Resources>
                <ControlTemplate TargetType="{x:Type RadioButton}" x:Key="ApprovalSelect">
                    <Border x:Name="Bor" Height="42" 
                            Background="Transparent"
                            BorderBrush="Gray" BorderThickness="0.5" CornerRadius="8"
                            Margin="0,20,0,0"
                            Tag="{TemplateBinding Tag}"
                            MouseLeftButtonDown="{local:MethodBinding BtnApprovalSelect}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                            </Grid.ColumnDefinitions>
                            <Image     Grid.Column="0" Source="{StaticResource Img_FolderOpenDoc}" 
                                       Width="20" Height="20"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       IsHitTestVisible="False"/>
                            <TextBlock Grid.Column="1" Text="{TemplateBinding Uid}" 
                                       FontSize="14"
                                       HorizontalAlignment="Left" VerticalAlignment="Center" 
                                       IsHitTestVisible="False"/>
                            <TextBlock Grid.Column="2"
                                       IsHitTestVisible="False"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger SourceName="Bor" Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bor" Property="BorderBrush" Value="{StaticResource Color_PointBlue}"/>
                            <Setter TargetName="Bor" Property="Background" Value="{StaticResource Color_WhiteGray}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Border.Resources>
            
            <StackPanel Orientation="Vertical" Margin="20">
                <Button Content="결재 작성하기" Height="42" FontSize="14" Click="{local:MethodBinding BtnApprovalAdd}"/>

                <RadioButton Uid="전체"      Tag="0" Template="{StaticResource ApprovalSelect}" IsChecked="True"/>
                <RadioButton Uid="결재 대기" Tag="1" Template="{StaticResource ApprovalSelect}"/>
                <RadioButton Uid="결재 진행" Tag="2" Template="{StaticResource ApprovalSelect}"/>
                <RadioButton Uid="결재 승인" Tag="3" Template="{StaticResource ApprovalSelect}"/>
                <RadioButton Uid="결재 거부" Tag="4" Template="{StaticResource ApprovalSelect}"/>
            </StackPanel>
        </Border>

        <!-- 결재 문서 목록 -->
        <ItemsControl Grid.Column="3" Grid.ColumnSpan="3" Grid.Row="4" ItemsSource="{Binding ApprovalList}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.Template>
                <ControlTemplate TargetType="{x:Type ItemsControl}">
                    <Border Style="{StaticResource BaseCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140" Tag="ID"/>
                                <ColumnDefinition Width="140" Tag="구분"/>
                                <ColumnDefinition Width="*"   Tag="내용"/>
                                <ColumnDefinition Width="140" Tag="기안자"/>
                                <ColumnDefinition Width="140" Tag="상태"/>
                                <ColumnDefinition Width="140" Tag="작성일"/>
                                <ColumnDefinition Width="140" Tag="수정일"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.Resources>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontSize" Value="14"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </Grid.Resources>

                            <Border Grid.Row="0" Grid.ColumnSpan="20" Background="{StaticResource Color_LightGray}" CornerRadius="8,8,0,0"/>
                            <TextBlock Grid.Column="0" Grid.Row="0" Text="ID"/>
                            <TextBlock Grid.Column="1" Grid.Row="0" Text="구분"/>
                            <TextBlock Grid.Column="2" Grid.Row="0" Text="내용"/>
                            <TextBlock Grid.Column="3" Grid.Row="0" Text="기안자"/>
                            <TextBlock Grid.Column="4" Grid.Row="0" Text="상태"/>
                            <TextBlock Grid.Column="5" Grid.Row="0" Text="작성일"/>
                            <TextBlock Grid.Column="6" Grid.Row="0" Text="수정일"/>

                            <Line Grid.Column="0" Grid.ColumnSpan="20" Grid.Row="0" Stroke="{StaticResource Color_Gray2}" StrokeThickness="1" X1="0" X2="2000" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"/>

                            <Line Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Stroke="{StaticResource Color_Gray2}" StrokeThickness="1" X1="0" Y2="2000" HorizontalAlignment="Right" VerticalAlignment="Stretch"/>
                            <Line Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Stroke="{StaticResource Color_Gray2}" StrokeThickness="1" X1="0" Y2="2000" HorizontalAlignment="Right" VerticalAlignment="Stretch"/>
                            <Line Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" Stroke="{StaticResource Color_Gray2}" StrokeThickness="1" X1="0" Y2="2000" HorizontalAlignment="Right" VerticalAlignment="Stretch"/>
                            <Line Grid.Column="3" Grid.Row="0" Grid.RowSpan="2" Stroke="{StaticResource Color_Gray2}" StrokeThickness="1" X1="0" Y2="2000" HorizontalAlignment="Right" VerticalAlignment="Stretch"/>
                            <Line Grid.Column="4" Grid.Row="0" Grid.RowSpan="2" Stroke="{StaticResource Color_Gray2}" StrokeThickness="1" X1="0" Y2="2000" HorizontalAlignment="Right" VerticalAlignment="Stretch"/>
                            <Line Grid.Column="5" Grid.Row="0" Grid.RowSpan="2" Stroke="{StaticResource Color_Gray2}" StrokeThickness="1" X1="0" Y2="2000" HorizontalAlignment="Right" VerticalAlignment="Stretch"/>
                            <Line Grid.Column="6" Grid.Row="0" Grid.RowSpan="2" Stroke="{StaticResource Color_Gray2}" StrokeThickness="1" X1="0" Y2="2000" HorizontalAlignment="Right" VerticalAlignment="Stretch"/>
                            <ScrollViewer Grid.Column="0" Grid.ColumnSpan="20" Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Hidden" Margin="0,0,0,20">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </ItemsControl.Template>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border x:Name="Bor" Height="40" Background="Transparent" BorderBrush="Transparent" BorderThickness="0.5" CornerRadius="4" 
                            MouseLeftButtonDown="{local:MethodBinding OnSelect}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140" Tag="ID"/>
                                <ColumnDefinition Width="140" Tag="구분"/>
                                <ColumnDefinition Width="*"   Tag="내용"/>
                                <ColumnDefinition Width="140" Tag="기안자"/>
                                <ColumnDefinition Width="140" Tag="상태"/>
                                <ColumnDefinition Width="140" Tag="작성일"/>
                                <ColumnDefinition Width="140" Tag="수정일"/>
                            </Grid.ColumnDefinitions>
                            <Grid.Resources>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontSize" Value="13"/>
                                </Style>
                            </Grid.Resources>


                            <TextBlock Grid.Column="0" Text="{Binding Id}"/>
                            <TextBlock Grid.Column="1" Text="{Binding Type}"/>
                            <TextBlock Grid.Column="2" Text="{Binding Title}"/>
                            <TextBlock Grid.Column="3" Text="{Binding Employee_id}"/>
                            <TextBlock Grid.Column="4" Text="{Binding Status}"/>
                            <TextBlock Grid.Column="5" Text="{Binding Created_at, StringFormat={}{0:yyyy-MM-dd}}"/>
                            <TextBlock Grid.Column="6" Text="{Binding Updated_at, StringFormat={}{0:yyyy-MM-dd}}"/>

                            <Line Grid.Column="0" Grid.ColumnSpan="20" Grid.Row="0" Stroke="{StaticResource Color_Gray2}" StrokeThickness="1" X1="0" X2="2000" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"/>
                        </Grid>
                    </Border>
                    <DataTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bor" Property="BorderBrush" Value="Blue"/>
                        </Trigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
