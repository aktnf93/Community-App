<UserControl x:Class="community.Views.V_SystemList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:community.Views"
             mc:Ignorable="d" 
             Width="1810" Height="950"
             Background="{StaticResource Color_WhiteGray}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30"  Tag="여백"/>
            <ColumnDefinition Width="280" Tag="그룹"/>
            <ColumnDefinition Width="30"  Tag="여백"/>
            <ColumnDefinition Width="*"   Tag="리스트"/>
            <ColumnDefinition Width="30"  Tag="여백"/>
            <ColumnDefinition Width="440" Tag="디테일"/>
            <ColumnDefinition Width="30"  Tag="여백"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"  Tag="타이틀"/>
            <RowDefinition Height="10"  Tag="여백"/>
            <RowDefinition Height="100" Tag="컨트롤"/>
            <RowDefinition Height="50"  Tag="여백"/>
            <RowDefinition Height="*"   Tag="리스트"/>
            <RowDefinition Height="30"  Tag="여백"/>
        </Grid.RowDefinitions>

        <!-- 타이틀 -->
        <Grid Grid.Column="1" Grid.ColumnSpan="5" Grid.Row="0" TextBlock.FontFamily="고도 M">
            <TextBlock Text="시스템 설정" FontSize="24" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
        </Grid>

        <Border Grid.Column="1" Grid.ColumnSpan="5" Grid.Row="2" Style="{StaticResource BaseCard}">

        </Border>

        <Border Grid.Column="1" Grid.ColumnSpan="1" Grid.Row="4" Style="{StaticResource BaseCard}">
            <ScrollViewer Grid.Column="1" Grid.Row="4" VerticalScrollBarVisibility="Hidden" Margin="10">
                <ItemsControl ItemsSource="{Binding SystemLogs}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel Orientation="Vertical" VirtualizingPanel.VirtualizationMode="Recycling"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Height="30" BorderThickness="0.5" BorderBrush="Gray" CornerRadius="8" Margin="0,0,10,0">
                                <Grid HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding Id}"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Category}"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Message}" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Grid.Column="3" Text="{Binding Created_At, StringFormat={}{0:yyMMdd HHmmss}}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>



        <ItemsControl Grid.Column="3" Grid.ColumnSpan="3" Grid.Row="4" ItemsSource="{Binding SystemConfigs}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical" VirtualizingPanel.VirtualizationMode="Recycling"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.Template>
                <ControlTemplate TargetType="{x:Type ItemsControl}">
                    <Border Style="{StaticResource BaseCard}">
                        <Grid>
                            <Grid.Resources>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontSize" Value="14"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </Grid.Resources>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="240"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="200"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Grid.ColumnSpan="20" Background="{StaticResource Color_LightGray}" CornerRadius="8,8,0,0"/>
                            <TextBlock Grid.Column="0" Grid.Row="0" Text="ID"/>
                            <TextBlock Grid.Column="1" Grid.Row="0" Text="이름"/>
                            <TextBlock Grid.Column="2" Grid.Row="0" Text="Num"/>
                            <TextBlock Grid.Column="3" Grid.Row="0" Text="Text"/>
                            <TextBlock Grid.Column="4" Grid.Row="0" Text="설명"/>
                            <TextBlock Grid.Column="5" Grid.Row="0" Text="업데이트"/>

                            <!-- 구분선 -->
                            <Line Grid.Column="0" Grid.ColumnSpan="8" Grid.Row="0" VerticalAlignment="Bottom" X1="0" X2="100" Stretch="Fill" StrokeThickness="0.5" Stroke="Gray"/>

                            <ScrollViewer Grid.Column="0" Grid.ColumnSpan="8" Grid.Row="1" VerticalScrollBarVisibility="Hidden">
                                <ItemsPresenter/>
                            </ScrollViewer>
                            <Line Grid.Column="0" Style="{StaticResource Line_Vertical}"/>
                            <Line Grid.Column="1" Style="{StaticResource Line_Vertical}"/>
                            <Line Grid.Column="2" Style="{StaticResource Line_Vertical}"/>
                            <Line Grid.Column="3" Style="{StaticResource Line_Vertical}"/>
                            <Line Grid.Column="4" Style="{StaticResource Line_Vertical}"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </ItemsControl.Template>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border x:Name="Bor" Height="40" Background="Transparent"
                            BorderBrush="Transparent" BorderThickness="0.5" CornerRadius="4">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="240"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="200"/>
                            </Grid.ColumnDefinitions>
                            <Grid.Resources>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontSize" Value="13"/>
                                </Style>
                            </Grid.Resources>

                            <TextBlock Grid.Column="0" Text="{Binding Id}"/>
                            <TextBlock Grid.Column="1" Text="{Binding Name}"         HorizontalAlignment="Left" Margin="10,0,0,0"/>
                            <TextBlock Grid.Column="2" Text="{Binding Value_Number}" HorizontalAlignment="Left" Margin="10,0,0,0"/>
                            <TextBlock Grid.Column="3" Text="{Binding Value_Text}"   HorizontalAlignment="Left" Margin="10,0,0,0"/>
                            <TextBlock Grid.Column="4" Text="{Binding Description}"  HorizontalAlignment="Left" Margin="10,0,0,0"/>
                            <TextBlock Grid.Column="5" Text="{Binding Updated_At, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}"/>

                            <Line Grid.Column="0" Grid.ColumnSpan="20" Grid.Row="0" Stroke="{StaticResource Color_Gray2}" StrokeThickness="1" X1="0" X2="2000" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"/>
                        </Grid>
                    </Border>
                    <DataTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bor" Property="BorderBrush" Value="Blue"/>
                        </Trigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
