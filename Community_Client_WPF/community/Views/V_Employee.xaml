<UserControl x:Class="community.Views.V_Employee"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:community.Views"
             xmlns:u="clr-namespace:community.View_Controls"
             xmlns:m="clr-namespace:community.Models"
             mc:Ignorable="d"  
             Width="1810" Height="1000" d:DesignWidth="1810" d:DesignHeight="1000"
             Loaded="{local:MethodBinding Loaded}" Background="{StaticResource Color_WhiteGray}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"  Tag="서브메뉴"/>
            <RowDefinition Height="*"   Tag="리스트"/>
        </Grid.RowDefinitions>

        <!-- 서브메뉴 -->
        <Border Grid.Row="0" TextBlock.FontFamily="고도 M" Background="{StaticResource Color_SubMenuBar}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Width="30"/>
                <RadioButton Content="직원 정보 관리" Template="{StaticResource SubMenuButton}" IsChecked="{Binding ChkListView}"/>
                <RadioButton Content="직원 근태 관리" Template="{StaticResource SubMenuButton}" IsChecked="{Binding ChkAtteendanceView}"/>
                <RadioButton Content="직원 일정 관리" Template="{StaticResource SubMenuButton}" IsChecked="{Binding ChkScheduleView}"/>
                <RadioButton Content="직원 휴가 관리" Template="{StaticResource SubMenuButton}" IsChecked="{Binding ChkLeaveView}"/>
                <RadioButton Content="직원 평가 관리" Template="{StaticResource SubMenuButton}" IsChecked="{Binding ChkReviewView}"/>
            </StackPanel>
        </Border>

        <local:V_EmployeeList       Grid.Row="1" Visibility="{Binding ChkListView,        Converter={StaticResource Conv_Visibility_Bool}}"/>
        <local:V_EmployeeAttendance Grid.Row="1" Visibility="{Binding ChkAtteendanceView, Converter={StaticResource Conv_Visibility_Bool}}"/>
        <local:V_EmployeeSchedule   Grid.Row="1" Visibility="{Binding ChkScheduleView,    Converter={StaticResource Conv_Visibility_Bool}}"/>
        <local:V_EmployeeLeave      Grid.Row="1" Visibility="{Binding ChkLeaveView,       Converter={StaticResource Conv_Visibility_Bool}}"/>
        <local:V_EmployeeReview     Grid.Row="1" Visibility="{Binding ChkReviewView,      Converter={StaticResource Conv_Visibility_Bool}}"/>
    </Grid>
</UserControl>
