<UserControl x:Class="community.Views.V_Employee"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:community.Views"
             xmlns:u="clr-namespace:community.View_Controls"
             xmlns:m="clr-namespace:community.Models"
             mc:Ignorable="d"  
             Width="1810" Height="1000" d:DesignWidth="1810" d:DesignHeight="1000"
             Loaded="{local:MethodBinding Loaded}" Background="{StaticResource Color_WhiteGray}">

    <Grid Margin="20,20,20,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="90"  Tag="배너"/>
            <RowDefinition Height="20"  Tag="여백"/>
            <RowDefinition Height="*"   Tag="리스트"/>
        </Grid.RowDefinitions>

        <!-- 배너 -->
        <Control Grid.Row="0" Template="{StaticResource BannerCard}" DataContext="{Binding BannerCard}"/>

        <Grid Grid.Row="2" Margin="-20,0,0,-30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" Tag="회사"/>
                <ColumnDefinition Width="200" Tag="부서"/>
                <ColumnDefinition Width="200" Tag="팀"/>
                <ColumnDefinition Width="*" Tag="직원"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Background="{StaticResource Color_ThinGray}">
                <StackPanel Orientation="Vertical">
                    <RadioButton Content="00회사"  Tag="{StaticResource Img_Menu_Home}" Template="{StaticResource MenuButton}"/>
                    <RadioButton Content="00전자"  Tag="{StaticResource Img_Menu_Home}" Template="{StaticResource MenuButton}"/>
                    <RadioButton Content="00마케팅" Tag="{StaticResource Img_Menu_Home}" Template="{StaticResource MenuButton}"/>
                    <RadioButton Content="00식품"  Tag="{StaticResource Img_Menu_Home}" Template="{StaticResource MenuButton}"/>
                    <RadioButton Content="00가공"  Tag="{StaticResource Img_Menu_Home}" Template="{StaticResource MenuButton}"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Background="{StaticResource Color_ThinGray2}">
                <StackPanel Orientation="Vertical">
                    <RadioButton Content="총무부"  Tag="{StaticResource Img_Menu_Home}" Template="{StaticResource MenuButton}"/>
                    <RadioButton Content="디자인부"  Tag="{StaticResource Img_Menu_Home}" Template="{StaticResource MenuButton}"/>
                    <RadioButton Content="개발부" Tag="{StaticResource Img_Menu_Home}" Template="{StaticResource MenuButton}"/>
                    <RadioButton Content="영업부"  Tag="{StaticResource Img_Menu_Home}" Template="{StaticResource MenuButton}"/>
                    <RadioButton Content="자재부"  Tag="{StaticResource Img_Menu_Home}" Template="{StaticResource MenuButton}"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" Background="{StaticResource Color_ThinGray3}">
                <StackPanel Orientation="Vertical">
                    <RadioButton Content="디자인 1팀"  Tag="{StaticResource Img_Menu_Home}" Template="{StaticResource MenuButton}"/>
                    <RadioButton Content="디자인 2팀"  Tag="{StaticResource Img_Menu_Home}" Template="{StaticResource MenuButton}"/>
                    <RadioButton Content="디자인 3팀" Tag="{StaticResource Img_Menu_Home}" Template="{StaticResource MenuButton}"/>
                    <RadioButton Content="아이콘 디자인"  Tag="{StaticResource Img_Menu_Home}" Template="{StaticResource MenuButton}"/>
                    <RadioButton Content="디자인 감독"  Tag="{StaticResource Img_Menu_Home}" Template="{StaticResource MenuButton}"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Column="3" Grid.Row="0">
                <u:U_TripleSwitch HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20"/>
            </Grid>

            <Grid Grid.Column="3" Grid.Row="1">

            </Grid>
        </Grid>

    </Grid>
</UserControl>
