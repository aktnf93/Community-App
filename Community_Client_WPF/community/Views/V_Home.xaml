<UserControl x:Class="community.Views.V_Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:community.Views"
             xmlns:m="clr-namespace:community.Models"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d" 
             Width="1810" Height="1000" d:DesignWidth="1810" d:DesignHeight="1000"
             Loaded="{local:MethodBinding Loaded}" Background="{StaticResource Color_WhiteGray}">
    
    <Grid Margin="20,20,20,30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" Tag="대시보드"/>
            <ColumnDefinition Width="20" Tag="여백"/>
            <ColumnDefinition Width="710" Tag="날씨뉴스"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="350"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Column="0" Grid.Row="0" Grid.RowSpan="3">
            <Grid.RowDefinitions>
                <RowDefinition Tag="프로필" Height="300"/>
                <RowDefinition Tag="공지사항" Height="100"/>
                <RowDefinition Tag="최근게시글"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource HomeBorder}" Margin="0,0,0,10">
                <Grid>
                    <Grid Width="90" Height="130" HorizontalAlignment="Left" VerticalAlignment="Top" DataContext="{Binding CurrentUser}">
                        <Ellipse Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="0" Width="70" Height="70" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="{Binding Image_FullPath}" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant"/>
                            </Ellipse.Fill>
                        </Ellipse>

                        <TextBlock Foreground="{StaticResource Color_White}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,10">
                        <Run Text="{Binding Id, StringFormat={}{0}: , Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Run Text="{Binding Name, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </TextBlock>

                        <Line VerticalAlignment="Bottom" X1="0" X2="100" Stretch="Fill" StrokeThickness="0.5" Stroke="Gray"/>
                    </Grid>
                    
                </Grid>
            </Border>
            <Border Grid.Row="1" Style="{StaticResource HomeBorder}"/>
            <Border Grid.Row="2" Style="{StaticResource HomeBorder}" Margin="0,10,0,0"/>
        </Grid>

        <Border Grid.Column="2" Grid.Row="0" Background="{StaticResource Color_Gray}" BorderBrush="Gray" BorderThickness="0.5">
            <wv2:WebView2 x:Name="WeatherWebView" Margin="2"/>
        </Border>

        <Border Grid.Column="2" Grid.Row="2" Background="{StaticResource Color_Gray}" BorderBrush="Gray" BorderThickness="0.5">
            <wv2:WebView2 x:Name="NewsWebView" Margin="2"/>
        </Border>
    </Grid>
</UserControl>
