<UserControl x:Class="community.Views.V_PostList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:community.Views"
             mc:Ignorable="d" 
             Width="1810" Height="950"
             Background="{StaticResource Color_WhiteGray}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30"  Tag="여백"/>
            <ColumnDefinition Width="280" Tag="그룹"/>
            <ColumnDefinition Width="30"  Tag="여백"/>
            <ColumnDefinition Width="*"   Tag="리스트"/>
            <ColumnDefinition Width="30"  Tag="여백"/>
            <ColumnDefinition Width="440" Tag="디테일"/>
            <ColumnDefinition Width="30"  Tag="여백"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"  Tag="타이틀"/>
            <RowDefinition Height="10"  Tag="여백"/>
            <RowDefinition Height="100" Tag="컨트롤"/>
            <RowDefinition Height="50"  Tag="여백"/>
            <RowDefinition Height="*"   Tag="리스트"/>
            <RowDefinition Height="30"  Tag="여백"/>
        </Grid.RowDefinitions>

        <!-- 타이틀 -->
        <Grid Grid.Column="1" Grid.ColumnSpan="5" Grid.Row="0" TextBlock.FontFamily="고도 M">
            <TextBlock Text="게시판" FontSize="24" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
        </Grid>

        <!-- 중간 컨트롤 -->
        <Border Grid.Column="1" Grid.ColumnSpan="5" Grid.Row="2" Style="{StaticResource BaseCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="검색옵션" FontSize="13" FontWeight="Bold" Width="50" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="320,0,0,0"/>
                    <CheckBox Content="전체" IsChecked="True" Margin="20,0,0,0"/>
                </StackPanel>

                <Line Grid.Row="0" Stroke="{StaticResource Color_Gray}" StrokeThickness="0.5" X1="0" X2="2000"
                      HorizontalAlignment="Stretch" VerticalAlignment="Bottom"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="검색명" FontSize="13" FontWeight="Bold" Width="50" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="320,0,0,0"/>

                    <ComboBox ItemsSource="{Binding CboListSearch}" SelectedValue="{Binding CboListSearchSelected}" Width="200" Height="28" Margin="20,0,0,0"/>
                    <TextBox Width="220" Height="28" ToolTip="검색어를 입력해주세요." Margin="20,0,0,0" Tag="{StaticResource Img_Edit}" Text="{Binding TxtListSearch}" Template="{StaticResource Icon_TextBox}"/>
                    <Button Width="70" Height="28" Content="검색" ToolTip="검색" Margin="20,0,0,0"  Tag="{StaticResource Img_Search_w}" Click="{local:MethodBinding BtnListSearch}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 그룹 리스트 -->
        <Border Grid.Column="1" Grid.Row="4" Style="{StaticResource BaseCard}">
            <TreeView Grid.Column="0" Grid.Row="5" ItemsSource="{Binding CategoryList}" Style="{StaticResource Tree_PostGroup}" Margin="10"/>
        </Border>

        <!-- 리스트 목록  -->
        <TabControl Grid.Column="3" Grid.ColumnSpan="3" Grid.Row="4" SelectedIndex="{Binding ListTabIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <TabItem Header="게시글 목록" Height="28" TabIndex="0">
                
                
                <ItemsControl ItemsSource="{Binding PostList}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel Orientation="Vertical" VirtualizingPanel.VirtualizationMode="Recycling"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.Template>
                        <ControlTemplate TargetType="{x:Type ItemsControl}">
                            <Border Style="{StaticResource BaseCard}">
                                <Grid>
                                    <Grid.Resources>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="FontSize" Value="14"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </Grid.Resources>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="40"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <Border Grid.Row="0" Grid.ColumnSpan="20" Background="{StaticResource Color_LightGray}" CornerRadius="8,8,0,0"/>
                                    <TextBlock Grid.Column="0" Grid.Row="0" Text="ID"/>
                                    <TextBlock Grid.Column="1" Grid.Row="0" Text="카테고리"/>
                                    <TextBlock Grid.Column="2" Grid.Row="0" Text="제목"/>
                                    <TextBlock Grid.Column="3" Grid.Row="0" Text="작성자"/>
                                    <TextBlock Grid.Column="4" Grid.Row="0" Text="작성일"/>
                                    <TextBlock Grid.Column="5" Grid.Row="0" Text="수정일"/>
                                    <TextBlock Grid.Column="6" Grid.Row="0" Text="조회수"/>
                                    <TextBlock Grid.Column="7" Grid.Row="0" Text="삭제"/>

                                    <!-- 구분선 -->
                                    <Line Grid.Column="0" Grid.ColumnSpan="9" Grid.Row="0" VerticalAlignment="Bottom" X1="0" X2="100" Stretch="Fill" StrokeThickness="0.5" Stroke="Gray"/>

                                    <ScrollViewer Grid.Column="0" Grid.ColumnSpan="9" Grid.Row="1" VerticalScrollBarVisibility="Hidden">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                    <Line  Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" HorizontalAlignment="Left" VerticalAlignment="Stretch" X1="0" Y2="100" Stretch="Fill" StrokeThickness="0.5" Stroke="Gray"/>
                                    <Line  Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" HorizontalAlignment="Right" VerticalAlignment="Stretch" X1="0" Y2="100" Stretch="Fill" StrokeThickness="0.5" Stroke="Gray"/>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </ItemsControl.Template>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="Bor" Height="30" Margin="0,0,0,0" Background="Transparent" 
                                    BorderBrush="Transparent" BorderThickness="0.5" CornerRadius="4" 
                                    MouseLeftButtonDown="{local:MethodBinding PostSelect}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.Resources>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="FontSize" Value="13"/>
                                        </Style>
                                    </Grid.Resources>

                                    <TextBlock Grid.Column="0" Text="{Binding Id}"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Post_Category_Name}"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Title}" HorizontalAlignment="Left" Margin="10,0,0,0"/>
                                    <TextBlock Grid.Column="3" Text="{Binding Employee_Name}"/>
                                    <TextBlock Grid.Column="4" Text="{Binding Created_At, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}"/>
                                    <TextBlock Grid.Column="5" Text="{Binding Updated_At, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}"/>
                                    <TextBlock Grid.Column="6" Text="{Binding View_Count}"/>

                                    <Line Grid.Column="0" Grid.ColumnSpan="10" Grid.Row="0" Stroke="{StaticResource Color_Gray2}" StrokeThickness="1" X1="0" X2="2000" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"/>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bor" Property="BorderBrush" Value="Blue"/>
                                </Trigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </TabItem>

            <TabItem Header="신규 생성" Height="28" TabIndex="1">
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="제목"/>
                    <TextBox Text="{Binding PostAdd.Title}" Width="500" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,0,30"/>
                    <TextBlock Text="본문"/>
                    <TextBox Text="{Binding PostAdd.Content}" Width="500" Height="400" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <Button Click="{local:MethodBinding AddPost}" Content="생성" Width="100" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,20,0,0"/>
                </StackPanel>
            </TabItem>

            <TabItem Header="{Binding PostSelected.Title}" Height="28" TabIndex="2">
                <TabItem.Style>
                    <Style TargetType="{x:Type TabItem}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ListTabIndex}" Value="2">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TabItem.Style>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <StackPanel Orientation="Vertical" Margin="0,0,30,0">
                        <TextBlock Text="제목"/>
                        <TextBox Text="{Binding PostSelected.Title}" Width="500" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,0,30"/>
                        <TextBlock Text="본문"/>
                        <TextBox Text="{Binding PostSelected.Content}" Width="500" Height="400" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                        <Button Click="{local:MethodBinding BtnPostEdit}" Content="수정" Width="100" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,20,0,0"/>
                    </StackPanel>

                    <StackPanel Margin="10" Orientation="Vertical">
                        <ScrollViewer VerticalScrollBarVisibility="Visible" Width="300" Height="420" Margin="0,0,0,0" 
                                      BorderBrush="Gray" BorderThickness="0.5">

                            <ItemsControl ItemsSource="{Binding PostSelected.CommentList}" 
                                          BorderBrush="Gray" BorderThickness="0.5">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel Orientation="Vertical"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="5" Padding="10" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Employee_Name}" FontWeight="Bold" FontSize="14"/>
                                                <TextBlock Text="{Binding Content}" TextWrapping="Wrap" Margin="0,5,0,0"/>
                                                <TextBlock Text="{Binding Created_At}" FontSize="12" Foreground="Gray" HorizontalAlignment="Right"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <TextBox Text="{Binding CommentAdd.Content}"  Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
                        <Button Content="댓글 추가" Click="{local:MethodBinding BtnCommentAdd}" Width="100" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,20,0,0"/>
                    </StackPanel>

                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
