<UserControl x:Class="community.Views.V_System"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:community.Views"
             xmlns:m="clr-namespace:community.Models"
             mc:Ignorable="d" 
             Width="1810" Height="1000" d:DesignWidth="1810" d:DesignHeight="1000"
             Loaded="{local:MethodBinding Loaded}" Background="{StaticResource Color_WhiteGray}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"  Tag="서브메뉴"/>
            <RowDefinition Height="*"   Tag="리스트"/>
        </Grid.RowDefinitions>

        <!-- 서브메뉴 -->
        <Border Grid.Row="0" TextBlock.FontFamily="고도 M" Background="{StaticResource Color_SubMenuBar}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Width="30"/>
                <RadioButton x:Name="RadSystem"   Content="시스템 설정"     Template="{StaticResource SubMenuButton}" IsChecked="{Binding ChkSystemListView}"/>
                <RadioButton x:Name="RadPost"     Content="게시판 기초코드" Template="{StaticResource SubMenuButton}" IsChecked="{Binding ChkSystemPostView}"/>
                <RadioButton x:Name="RadEmployee" Content="직원 기초코드"   Template="{StaticResource SubMenuButton}" IsChecked="{Binding ChkSystemEmployeeView}"/>
            </StackPanel>
        </Border>

        <local:V_SystemList     Grid.Row="1" Visibility="{Binding ChkSystemListView, Converter={StaticResource Conv_Visibility_Bool}}"/>
        <local:V_SystemPost     Grid.Row="1" Visibility="{Binding ChkSystemPostView, Converter={StaticResource Conv_Visibility_Bool}}"/>
        <local:V_SystemEmployee Grid.Row="1" Visibility="{Binding ChkSystemEmployeeView, Converter={StaticResource Conv_Visibility_Bool}}"/>
    </Grid>
</UserControl>
