<UserControl x:Class="community.Views.V_System"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:community.Views"
             xmlns:m="clr-namespace:community.Models"
             mc:Ignorable="d" 
             Width="1810" Height="1000" d:DesignWidth="1810" d:DesignHeight="1000"
             Loaded="{local:MethodBinding Loaded}" Background="{StaticResource Color_WhiteGray}">

    <Grid Margin="20,20,20,30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" Tag="카테고리"/>
            <ColumnDefinition Width="60"  Tag="여백"/>
            <ColumnDefinition Width="*"   Tag="리스트"/>
            <ColumnDefinition Width="60"  Tag="여백"/>
            <ColumnDefinition Width="240" Tag="최근이력"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="90"  Tag="배너"/>
            <RowDefinition Height="20"  Tag="여백"/>
            <RowDefinition Height="90"  Tag="대시보드"/>
            <RowDefinition Height="20"  Tag="여백"/>
            <RowDefinition Height="40"  Tag="컨트롤러"/>
            <RowDefinition Height="*"   Tag="리스트"/>
        </Grid.RowDefinitions>

        <!-- 배너 -->
        <Control Grid.Column="0" Grid.ColumnSpan="5" Grid.Row="0" Template="{StaticResource BannerCard}" DataContext="{Binding BannerCard}"/>

        <!-- 대시보드 카드 -->
        <StackPanel Grid.Column="2" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Control Tag="{StaticResource Img_System_w}" Template="{StaticResource DashboardCard}" DataContext="{Binding DashboardCard[0]}" Margin="0,0,20,0"/>
            <Control Tag="{StaticResource Img_System_w}" Template="{StaticResource DashboardCard}" DataContext="{Binding DashboardCard[1]}" Margin="0,0,20,0"/>
            <Control Tag="{StaticResource Img_System_w}" Template="{StaticResource DashboardCard}" DataContext="{Binding DashboardCard[2]}" Margin="0,0,20,0"/>
            <Control Tag="{StaticResource Img_System_w}" Template="{StaticResource DashboardCard}" DataContext="{Binding DashboardCard[3]}" Margin="0,0,20,0"/>
            <Control Tag="{StaticResource Img_System_w}" Template="{StaticResource DashboardCard}" DataContext="{Binding DashboardCard[4]}"/>
        </StackPanel>

        <!-- 카테고리 제어 -->
        <Grid Grid.Column="0" Grid.Row="4">
            <Control Template="{StaticResource CategorySearch}" ToolTip="검색"/>
        </Grid>

        <!-- 리스트 제어 -->
        <Grid Grid.Column="2" Grid.Row="4">
            <Control Template="{StaticResource ListSearch}" ToolTip="검색"/>
        </Grid>

        <!-- 카테고리 목록 -->
        <TabControl Grid.Column="0" Grid.Row="5">
            <TabItem Header="목록" Height="28">
                <TreeView Grid.Column="0" Grid.Row="5" ItemsSource="{Binding CategoryList}" Style="{StaticResource CategoryView}"/>
            </TabItem>
        </TabControl>

        <!-- 리스트 목록  -->
        <TabControl Grid.Column="2" Grid.Row="5">
            <TabItem Header="시스템 항목" Height="28">
                <ItemsControl ItemsSource="{Binding SystemConfigs}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel Orientation="Vertical" VirtualizingPanel.VirtualizationMode="Recycling"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.Template>
                        <ControlTemplate TargetType="{x:Type ItemsControl}">
                            <Border BorderBrush="Gray" BorderThickness="0.5" Padding="0,0,0.5,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="240"/>
                                        <ColumnDefinition Width="300"/>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="17"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="30"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="53"/>
                                    </Grid.RowDefinitions>

                                    <Label Grid.Column="0" Grid.Row="0" Content="ID" Style="{StaticResource Lable_Column}"/>
                                    <Label Grid.Column="1" Grid.Row="0" Content="이름" Style="{StaticResource Lable_Column}"/>
                                    <Label Grid.Column="2" Grid.Row="0" Content="Num" Style="{StaticResource Lable_Column}"/>
                                    <Label Grid.Column="3" Grid.Row="0" Content="Text" Style="{StaticResource Lable_Column}"/>
                                    <Label Grid.Column="4" Grid.Row="0" Content="설명" Style="{StaticResource Lable_Column}"/>
                                    <Label Grid.Column="5" Grid.Row="0" Content="업데이트" Style="{StaticResource Lable_Column}"/>

                                    <!-- 구분선 -->
                                    <Line Grid.Column="0" Grid.ColumnSpan="8" Grid.Row="0" VerticalAlignment="Bottom" X1="0" X2="100" Stretch="Fill" StrokeThickness="0.5" Stroke="Gray"/>

                                    <ScrollViewer Grid.Column="0" Grid.ColumnSpan="8" Grid.Row="1" VerticalScrollBarVisibility="Visible">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                    <Line Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" HorizontalAlignment="Right" VerticalAlignment="Stretch" X1="0" Y2="100" Stretch="Fill" StrokeThickness="0.5" Stroke="Gray"/>
                                    <Line Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" HorizontalAlignment="Right" VerticalAlignment="Stretch" X1="0" Y2="100" Stretch="Fill" StrokeThickness="0.5" Stroke="Gray"/>
                                    <Line Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" HorizontalAlignment="Right" VerticalAlignment="Stretch" X1="0" Y2="100" Stretch="Fill" StrokeThickness="0.5" Stroke="Gray"/>
                                    <Line Grid.Column="3" Grid.Row="0" Grid.RowSpan="2" HorizontalAlignment="Right" VerticalAlignment="Stretch" X1="0" Y2="100" Stretch="Fill" StrokeThickness="0.5" Stroke="Gray"/>
                                    <Line Grid.Column="4" Grid.Row="0" Grid.RowSpan="2" HorizontalAlignment="Right" VerticalAlignment="Stretch" X1="0" Y2="100" Stretch="Fill" StrokeThickness="0.5" Stroke="Gray"/>

                                    <!-- 페이지 -->
                                    <Grid Grid.Column="0"  Grid.ColumnSpan="9" Grid.Row="2">
                                        <Line VerticalAlignment="Top" X1="0" X2="100" Stretch="Fill" StrokeThickness="0.5" Stroke="Gray"/>
                                    </Grid>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </ItemsControl.Template>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="Gray" BorderThickness="0.5" Height="30" Cursor="Hand">
                                <Border.Style>
                                    <Style TargetType="{x:Type Border}">
                                        <Setter Property="Background" Value="White"/>
                                        <Setter Property="TextBlock.Foreground" Value="Black"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="Gray"/>
                                                <Setter Property="TextBlock.Foreground" Value="White"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <Grid VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="240"/>
                                        <ColumnDefinition Width="300"/>
                                        <ColumnDefinition Width="140"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding Id}" HorizontalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Name}"         Margin="10,0,0,0"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Value_Number}" Margin="10,0,0,0"/>
                                    <TextBlock Grid.Column="3" Text="{Binding Value_Text}"   Margin="10,0,0,0"/>
                                    <TextBlock Grid.Column="4" Text="{Binding Description}"  Margin="10,0,0,0"/>
                                    <TextBlock Grid.Column="5" Text="{Binding Updated_At, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" HorizontalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </TabItem>
        </TabControl>

        <!-- 최근이력 목록 -->
        <TabControl Grid.Column="4" Grid.Row="5">
            <TabItem Header="최근 이력" Height="28">
                <ItemsControl>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel Orientation="Vertical" VirtualizingPanel.VirtualizationMode="Recycling"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.Template>
                        <ControlTemplate TargetType="{x:Type ItemsControl}">
                            <Border BorderBrush="Gray" BorderThickness="0.5" Padding="0,0,0.5,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="17"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="0" MaxHeight="30"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="53"/>
                                    </Grid.RowDefinitions>

                                    <!--<Label Grid.Column="0" Grid.Row="0" Content="최근 이력" Style="{StaticResource Lable_Column}"/>
                            <Line  Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" VerticalAlignment="Bottom" X1="0" X2="100" Stretch="Fill" StrokeThickness="0.5" Stroke="Gray"/>-->

                                    <ScrollViewer Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" VerticalScrollBarVisibility="Visible"
                                          Background="{StaticResource Color_White}">
                                        <ItemsPresenter/>
                                    </ScrollViewer>

                                    <Grid Grid.Column="0"  Grid.ColumnSpan="9" Grid.Row="2">
                                        <Line VerticalAlignment="Top" X1="0" X2="100" Stretch="Fill" StrokeThickness="0.5" Stroke="Gray"/>

                                        <StackPanel Grid.Column="0" Grid.ColumnSpan="9" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </ItemsControl.Template>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Height="30" BorderThickness="0.5" BorderBrush="Gray">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock x:Name="Title" Grid.Column="0" Text="{Binding Title}" TextTrimming="CharacterEllipsis" 
                                       HorizontalAlignment="Left" VerticalAlignment="Center" Margin="8,0,0,0"
                                       TextDecorations="Underline"/>

                                    <TextBlock x:Name="Update" Grid.Column="1" Text="{Binding Updated_At, StringFormat={}{0:yyyy-MM-dd}}" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       TextDecorations="Underline"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
